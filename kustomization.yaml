apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: app-
metadata:
  name: crawlee-ressources
commonLabels:
  app: crawlee

resources:
- ./base

secretGenerator:
- name: crawlee-secret
  envs:
  - secret.env

configMapGenerator:
- name: crawlee-config
  envs:
  - configmap.env